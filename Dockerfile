# Используем официальный образ OpenJDK 11 в качестве базового образа
FROM openjdk:18-alpine

# Копируем все файлы в текущей директории в образ
COPY . /app

# Устанавливаем рабочую директорию для приложения
WORKDIR /app

# Собираем приложение
#RUN ./mvnw package

# Указываем, что приложение будет работать на порту 8080
EXPOSE 8080

# Запускаем приложение при старте контейнера
CMD ["java", "-jar", "target/spring-boot_security-demo-0.0.1-SNAPSHOT.jar"]